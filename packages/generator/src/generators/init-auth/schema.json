{
  "$schema": "https://json-schema.org/schema",
  "$id": "InitAuth",
  "title": "Initialize AnalogJS Authentication",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Project where authentication should be initialized",
      "alias": "p",
      "x-prompt": "Which project should have authentication initialized?",
      "x-dropdown": "projects"
    }
  },
  "required": ["project"]
}
