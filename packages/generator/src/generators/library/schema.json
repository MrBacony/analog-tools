{
  "$schema": "https://json-schema.org/schema",
  "$id": "Library",
  "title": "Generate a Library with module routing",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "How should the library be named?",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use?",
      "x-priority": "important"
    },
    "project": {
      "type": "string",
      "description": "Which application project should this library be added to?",
      "alias": "p",
      "x-prompt": "What is the name of the application project?",
      "x-dropdown": "projects",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "x-priority": "important"
    },
    "pages": {
      "type": "boolean",
      "description": "Should the library include pages?",
      "x-prompt": {
        "type": "confirmation",
        "message": "Do you want this library to include pages?"
      },
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "x-priority": "important"
    },
    "api": {
      "type": "boolean",
      "description": "Should the library include API routes?",
      "x-prompt": {
        "type": "confirmation",
        "message": "Do you want this library to include API routes?"
      },
      "x-priority": "important"
    },
    "contentRoutes": {
      "type": "boolean",
      "description": "Should the library include content routes?",
      "x-prompt": {
        "type": "confirmation",
        "message": "Do you want this library to include content routes?"
      },
      "x-priority": "important"
    },
    "trpc": {
      "type": "boolean",
      "description": "Should the library include a trpc router?",
      "x-prompt": {
        "type": "confirmation",
        "message": "Do you want this library to include a tRPC router?"
      }
    },
    "skipExamples": {
      "type": "boolean",
      "description": "Skip generating example files (component, model, pages)?",
      "default": false,
      "x-prompt": {
        "type": "confirmation",
        "message": "Do you want to skip generating example files?"
      }
    },
    "componentPrefix": {
      "type": "string",
      "description": "The prefix to use for component selectors",
      "default": "lib",
      "x-prompt": "What prefix would you like to use for component selectors?"
    },
    "patchTailwind": {
      "type": "boolean",
      "description": "Patch the Tailwind CSS import in the project to include library sources",
      "default": true
    }
  },
  "required": ["name", "project"]
}
